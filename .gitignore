# Build output
dist/
node_modules/

# Vercel
.vercel

# Env files (may contain secrets)
.env
.env.local
.env*.local

# Claude Code per-directory settings (may contain local paths, tokens, PATs)
.claude/

# Playwright MCP QA artifacts
.playwright-mcp/

# Weekly payroll drops — employee-level pay data, keep out of git (workflow re-drops them)
incoming-freightiq/
# QA screenshots + scratch parse artifacts
/*.png
/sc_clean.py
/sc_u.txt
scripts/_*.txt
scripts/_*.js

# Local DB creds for scripts/dbrun.mjs (session pooler) — NEVER commit
.env.db
scripts/_*.json
scripts/_*.log
